[project]
name = "qubes-core-admin-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lxml>=6.0.2",
]

[dependency-groups]
dev = [
    "ty>=0.0.16",
    "types-lxml>=2026.1.1",
]
